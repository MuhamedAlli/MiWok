<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="feed.r.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/family_older_brother"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">







        <activity android:name=".Phrases" android:label="Pharses"></activity>
        <activity android:name=".Colors" android:label="Colors" />
        <activity android:name=".Numbers" android:label="Numbers" />
        <activity android:name=".Family" android:label="Family Members"/>
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>